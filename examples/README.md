# Examples

每个目录对应 README 中的一个学习阶段。阶段示例先保持独立可运行；当逻辑稳定且重复出现时，再抽到 `src/agno_learn/`。

推荐每个阶段目录包含：

- `README.md`：目标、运行命令、观察点、失败记录。
- `main.py`：最小可运行入口。
- `fixtures/`：小型输入资料或 prompt 样例。

当前目录先作为代码规划骨架，后续按阶段补充 `main.py`。
