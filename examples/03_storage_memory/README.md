# 03 Storage And Memory

目标：用 SQLite 保存 session history，并尝试 automatic memory 与 agentic memory。

计划文件：

- `main.py`：带数据库和 memory 的 Agent。
- `inspect_memory.py`：读取并查看某个 `user_id` 的 memory。

官方入口：[What is Memory?](https://docs.agno.com/memory/overview.md)
