# Concepts

Use this note to summarize AgentScope concepts as each stage is completed.

## Core Relationship

- `Model` calls the underlying provider.
- `Toolkit` exposes tools to the model.
- `Agent` coordinates context, model calls, tool calls, events, and final messages.
